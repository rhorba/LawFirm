# Financial Sync, Search, Audit Snapshots & Case-Client Link — Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: use executing-plans skill to implement this plan task-by-task.

**Goal:** Fix 4 bugs (searchable dropdowns, audit before/after, case-client UI, invoice-transaction sync) and implement 2 data seeds (full tribunals/case types from Excel, financial transaction test data).

**Architecture:** 7 Flyway migrations lay the DB foundation, then backend services/controllers are extended, and finally Angular standalone components are added or modified. All changes follow strict layered hexagonal: entity → repository → service → controller → frontend service → component.

**Tech Stack:** Spring Boot 3.4 (Java 21), Angular 18 standalone, Flyway, MapStruct, Tailwind CSS.

---

## Task 1 — V56: Add invoice_id FK to financial_transactions

**Files:**
- Create: `backend/src/main/resources/db/migration/V56__add_invoice_id_to_financial_transactions.sql`

**Step 1: Write migration**

```sql
-- V56__add_invoice_id_to_financial_transactions.sql
ALTER TABLE financial_transactions
    ADD COLUMN invoice_id BIGINT,
    ADD CONSTRAINT fk_financial_transactions_invoice
        FOREIGN KEY (invoice_id) REFERENCES invoices(id) ON DELETE SET NULL,
    ADD CONSTRAINT uq_financial_transactions_invoice
        UNIQUE (invoice_id);

COMMENT ON COLUMN financial_transactions.invoice_id
    IS 'When set, this transaction was auto-generated by marking the linked invoice as PAID. UNIQUE ensures one transaction per invoice.';
```

**Step 2: Verify**
```bash
mvn flyway:migrate -pl backend
# Expect: Successfully applied 1 migration
```

---

## Task 2 — V57: Replace tribunal seeds (125 real Moroccan courts)

**Files:**
- Create: `backend/src/main/resources/db/migration/V57__replace_tribunal_seeds.sql`

**Step 1: Write migration**

Use UPSERT (not TRUNCATE) because existing cases may reference current tribunal codes via FK. Apostrophes in French names must be doubled (`''`).

```sql
-- V57__replace_tribunal_seeds.sql
-- Upsert all 125 Moroccan courts from official Excel reference list.
-- Uses ON CONFLICT (code) DO UPDATE to safely update existing records without breaking FK references from cases table.

INSERT INTO tribunals (code, name_fr, name_ar, active) VALUES
('TR_ADM_APPL_1', 'Tribunal d''appel administratif de Rabat', 'محكمة الاستئناف الإدارية بالرباط', true),
('TR_ADM_APPL_2', 'Tribunal d''appel administratif de Marrakech', 'محكمة الإستئناف الإدارية بمراكش', true),
('TR_ADM_PIN_1', 'Tribunal administratif de Rabat', 'المحكمة الإدارية بالرباط', true),
('TR_ADM_PIN_2', 'Tribunal administratif de Casablanca', 'المحكمة الإدارية بالدار البيضاء', true),
('TR_ADM_PIN_3', 'Tribunal administratif de Fes', 'المحكمة الإدارية بفاس', true),
('TR_ADM_PIN_4', 'Tribunal administratif de Meknes', 'المحكمة الإدارية بمكناس', true),
('TR_ADM_PIN_5', 'Tribunal administratif de Oujda', 'المحكمة الإدارية بوجدة', true),
('TR_ADM_PIN_6', 'Tribunal administratif de Marrakech', 'المحكمة الإدارية بمراكش', true),
('TR_ADM_PIN_7', 'Tribunal administratif de Agadir', 'المحكمة الإدارية بأكادير', true),
('TR_COM_APPL_1', 'Tribunal d''appel commercial de Casablanca', 'محكمة الاستئناف التجارية - الدار البيضاء', true),
('TR_COM_APPL_2', 'Tribunal d''appel commercial de Marrakech', 'محكمة الاستئناف التجارية - مراكش', true),
('TR_COM_APPL_3', 'Tribunal d''appel commercial de Fes', 'محكمة الاستئناف التجارية - فاس', true),
('TR_COM_PIN_1', 'Tribunal commercial de Rabat', 'المحكمة التجارية - الرباط', true),
('TR_COM_PIN_2', 'Tribunal commercial de Casablanca', 'المحكمة التجارية - الدار البيضاء', true),
('TR_COM_PIN_3', 'Tribunal commercial de Agadir', 'المحكمة التجارية - أكادير', true),
('TR_COM_PIN_4', 'Tribunal commercial de Marrakech', 'المحكمة التجارية مراكش', true),
('TR_COM_PIN_5', 'Tribunal commercial de Fes', 'المحكمة التجارية بفاس', true),
('TR_COM_PIN_6', 'Tribunal commercial de Meknes', 'المحكمة التجارية بمكناس', true),
('TR_COM_PIN_7', 'Tribunal commercial de Oujda', 'المحكمة التجارية بوجدة', true),
('TR_COM_PIN_8', 'Tribunal commercial de Tanger', 'المحكمة التجارية بطنجة', true),
('TR_APPL_1', 'Tribunal d''appel de Rabat', 'محكمة الاستئناف - بالرباط', true),
('TR_APPL_2', 'Tribunal d''appel de Laayoune', 'محكمة الاستئناف العيون', true),
('TR_APPL_3', 'Tribunal d''appel de Agadir', 'محكمة الاستئناف أكادير', true),
('TR_APPL_4', 'Tribunal d''appel de Ouarzazate', 'محكمة الاستئناف ورزازات', true),
('TR_APPL_5', 'Tribunal d''appel de Kenitra', 'محكمة الاستئناف القنيطرة', true),
('TR_APPL_6', 'Tribunal d''appel de Settat', 'محكمة الاستئناف سطات', true),
('TR_APPL_7', 'Tribunal d''appel de Khouribga', 'محكمة الاستئناف - خريبكة', true),
('TR_APPL_8', 'Tribunal d''appel de Casablanca', 'محكمة الاستئناف - الدار البيضاء', true),
('TR_APPL_9', 'Tribunal d''appel de Marrakech', 'محكمة الاستئناف - مراكش', true),
('TR_APPL_10', 'Tribunal d''appel de Safi', 'محكمة الاستئناف - آسفي', true),
('TR_APPL_11', 'Tribunal d''appel de Tanger', 'محكمة الاستئناف - طنجة', true),
('TR_APPL_12', 'Tribunal d''appel de Tetouan', 'محكمة الاستئناف - تطوان', true),
('TR_APPL_13', 'Tribunal d''appel de Al Hoceima', 'محكمة الاستئناف - الحسيمة', true),
('TR_APPL_14', 'Tribunal d''appel de Taza', 'محكمة الاستئناف - تازة', true),
('TR_APPL_15', 'Tribunal d''appel de Oujda', 'محكمة الاستئناف - وجدة', true),
('TR_APPL_16', 'Tribunal d''appel de Meknes', 'محكمة الاستئناف - مكناس', true),
('TR_APPL_17', 'Tribunal d''appel de El Jadida', 'محكمة الاستئناف - الجديدة', true),
('TR_APPL_18', 'Tribunal d''appel de Beni Mellal', 'محكمة الاستئناف - بني ملال', true),
('TR_APPL_19', 'Tribunal d''appel de Errachidia', 'محكمة الاستئناف - الرشيدية', true),
('TR_APPL_20', 'Tribunal d''appel de Nador', 'محكمة الاستئناف - الناظور', true),
('TR_APPL_21', 'Tribunal d''appel de Fes', 'محكمة الاستئناف - فاس', true),
('TR_APPL_22', 'Tribunal d''appel de Guelmim', 'محكمة الاستئناف - كلميم', true),
('TR_PIN_1', 'Tribunal de 1ère instance de Rabat', 'المحكمة الابتدائية - الرباط', true),
('TR_PIN_2', 'Tribunal de 1ère instance de Salé', 'المحكمة الابتدائية سلا', true),
('TR_PIN_3', 'Tribunal de 1ère instance de Temara', 'المحكمة الابتدائية - تمارة', true),
('TR_PIN_4', 'Tribunal de 1ère instance de Khemisset', 'المحكمة الابتدائية - الخميسات', true),
('TR_PIN_5', 'Tribunal de 1ère instance de Rommani', 'المحكمة الابتدائية - الرماني', true),
('TR_PIN_6', 'Tribunal de 1ère instance de Tiflet', 'المحكمة الابتدائية - تيفلت', true),
('TR_PIN_7', 'Tribunal de 1ère instance de Oued Eddahab', 'المحكمة الابتدائية وادي الذهب', true),
('TR_PIN_8', 'Tribunal de 1ère instance de Laayoune', 'المحكمة الابتدائية العيون', true),
('TR_PIN_9', 'Tribunal de 1ère instance de Essmara', 'المحكمة الابتدائية السمارة', true),
('TR_PIN_10', 'Tribunal de 1ère instance de Tata', 'المحكمة الابتدائية طاطا', true),
('TR_PIN_11', 'Tribunal de 1ère instance de Inezgane', 'المحكمة الابتدائية إنزكان', true),
('TR_PIN_12', 'Tribunal de 1ère instance de Biougra', 'المحكمة الابتدائية بيوكرى', true),
('TR_PIN_13', 'Tribunal de 1ère instance de Taroudant', 'المحكمة الابتدائية تارودانت', true),
('TR_PIN_14', 'Tribunal de 1ère instance de Tiznit', 'المحكمة الابتدائية تيزنيت', true),
('TR_PIN_15', 'Tribunal de 1ère instance de Agadir', 'المحكمة الابتدائية أكادير', true),
('TR_PIN_16', 'Tribunal de 1ère instance de Ouarzazate', 'المحكمة الابتدائية ورزازات', true),
('TR_PIN_17', 'Tribunal de 1ère instance de Zagora', 'المحكمة الابتدائية زاكورة', true),
('TR_PIN_18', 'Tribunal de 1ère instance de Tinghir', 'المحكمة الابتدائية تنغير', true),
('TR_PIN_19', 'Tribunal de 1ère instance de Kenitra', 'المحكمة الابتدائية القنيطرة', true),
('TR_PIN_20', 'Tribunal de 1ère instance de Sidi Slimane', 'المحكمة الابتدائية سيدي سليمان', true),
('TR_PIN_21', 'Tribunal de 1ère instance de Souk El Arbaa', 'المحكمة الابتدائية سوق أربعاء الغرب', true),
('TR_PIN_22', 'Tribunal de 1ère instance de Sidi Kacem', 'المحكمة الابتدائية سيدي قاسم', true),
('TR_PIN_23', 'Tribunal de 1ère instance de Mechra Bel Ksiri', 'المحكمة الابتدائية مشرع بلقصيري', true),
('TR_PIN_24', 'Tribunal de 1ère instance de Settat', 'المحكمة الابتدائية - سطات', true),
('TR_PIN_25', 'Tribunal de 1ère instance de Berchid', 'المحكمة الابتدائية - برشيد', true),
('TR_PIN_26', 'Tribunal de 1ère instance de Ben Ahmed', 'المحكمة الابتدائية - بن أحمد', true),
('TR_PIN_27', 'Tribunal de 1ère instance de Khouribga', 'المحكمة الابتدائية - خريبكة', true),
('TR_PIN_28', 'Tribunal de 1ère instance de Oued Zem', 'المحكمة الابتدائية - وادي زم', true),
('TR_PIN_29', 'Tribunal de 1ère instance de Bejaad', 'المحكمة الابتدائية - أبي الجعد', true),
('TR_PIN_30', 'Tribunal civile de Benslimane', 'المحكمة الابتدائية - بنسليمان', true),
('TR_PIN_31', 'Tribunal de 1ère instance civile de Casablanca', 'المحكمة الابتدائية المدنية بالدار البيضاء', true),
('TR_PIN_32', 'Tribunal de 1ère instance de Bouznika', 'المحكمة الابتدائية - بوزنيقة', true),
('TR_PIN_33', 'Tribunal civile de Mohamedia', 'المحكمة الابتدائية - المحمدية', true),
('TR_PIN_34', 'Tribunal correctionnel de Casablanca', 'المحكمة الابتدائية الزجرية الدار البيضاء', true),
('TR_PIN_36', 'Tribunal de 1ère instance de Imintanout', 'المحكمة الابتدائية إيمنتانوت', true),
('TR_PIN_37', 'Tribunal de 1ère instance de Marrakech', 'المحكمة الابتدائية - مراكش', true),
('TR_PIN_38', 'Tribunal de 1ère instance de El Kelaa des Sraghna', 'المحكمة الابتدائية - قلعة السراغنة', true),
('TR_PIN_39', 'Tribunal de 1ère instance de Ben Guerir', 'المحكمة الابتدائية - ابن جرير', true),
('TR_PIN_40', 'Tribunal de 1ère instance de Tahanaout', 'المحكمة الابتدائية - تحناوت', true),
('TR_PIN_41', 'Tribunal de 1ère instance de Essaouira', 'المحكمة الابتدائية - الصويرة', true),
('TR_PIN_42', 'Tribunal de 1ère instance de Safi', 'المحكمة الابتدائية - آسفي', true),
('TR_PIN_43', 'Tribunal de 1ère instance de Youssoufia', 'المحكمة الابتدائية - اليوسفية', true),
('TR_PIN_44', 'Tribunal de 1ère instance de Tanger', 'المحكمة الابتدائية - طنجة', true),
('TR_PIN_45', 'Tribunal de 1ère instance de Asilah', 'المحكمة الابتدائية - أصيلة', true),
('TR_PIN_46', 'Tribunal de 1ère instance de Larache', 'المحكمة الابتدائية - العرائش', true),
('TR_PIN_47', 'Tribunal de 1ère instance de Ksar El Kebir', 'المحكمة الابتدائية - القصر الكبير', true),
('TR_PIN_48', 'Tribunal de 1ère instance de Ouazzane', 'المحكمة الابتدائية وزان', true),
('TR_PIN_49', 'Tribunal de 1ère instance de Tetouan', 'المحكمة الابتدائية - تطوان', true),
('TR_PIN_50', 'Tribunal de 1ère instance de Chefchaouen', 'المحكمة الابتدائية - شفشاون', true),
('TR_PIN_51', 'Tribunal de 1ère instance de Mdiq', 'المحكمة الابتدائية - المضيق', true),
('TR_PIN_52', 'Tribunal de 1ère instance de Al Hoceima', 'المحكمة الابتدائية - الحسيمة', true),
('TR_PIN_53', 'Tribunal de 1ère instance de Targuist', 'المحكمة الابتدائية - تارجيست', true),
('TR_PIN_54', 'Tribunal de 1ère instance de Taza', 'المحكمة الابتدائية - تازة', true),
('TR_PIN_55', 'Tribunal de 1ère instance de Oujda', 'المحكمة الابتدائية - وجدة', true),
('TR_PIN_56', 'Tribunal de 1ère instance de Berkane', 'المحكمة الابتدائية - بركان', true),
('TR_PIN_57', 'Tribunal de 1ère instance de Guercif', 'المحكمة الابتدائية - جرسيف', true),
('TR_PIN_58', 'Tribunal de 1ère instance de Taourirt', 'المحكمة الابتدائية - تاوريرت', true),
('TR_PIN_59', 'Tribunal de 1ère instance de Jrada', 'المحكمة الابتدائية - جرادة', true),
('TR_PIN_60', 'Tribunal de 1ère instance de Figuig Bouarfa', 'المحكمة الابتدائية - فجيج', true),
('TR_PIN_61', 'Tribunal de 1ère instance de Meknes', 'المحكمة الابتدائية - مكناس', true),
('TR_PIN_62', 'Tribunal de 1ère instance de Hajeb', 'المحكمة الابتدائية - الحاجب', true),
('TR_PIN_63', 'Tribunal de 1ère instance de Azrou', 'المحكمة الابتدائية - آزرو', true),
('TR_PIN_64', 'Tribunal de 1ère instance de El Jadida', 'المحكمة الابتدائية الجديدة', true),
('TR_PIN_65', 'Tribunal de 1ère instance de Sidi Bennour', 'المحكمة الابتدائية سيدي بنور', true),
('TR_PIN_66', 'Tribunal de 1ère instance de Beni Mellal', 'المحكمة الابتدائية - بني ملال', true),
('TR_PIN_67', 'Tribunal de 1ère instance de Azilal', 'المحكمة الابتدائية - أزيلال', true),
('TR_PIN_68', 'Tribunal de 1ère instance de Fquih Ben Salah', 'المحكمة الابتدائية - الفقيه بن صالح', true),
('TR_PIN_69', 'Tribunal de 1ère instance de Khenifra', 'المحكمة الابتدائية - خنيفرة', true),
('TR_PIN_70', 'Tribunal de 1ère instance de Kasba Tadla', 'المحكمة الابتدائية - قصبة تادلة', true),
('TR_PIN_71', 'Tribunal de 1ère instance de Souk Sebt Oulad Nemma', 'المحكمة الابتدائية - سوق السبت أولاد النمة', true),
('TR_PIN_72', 'Tribunal de 1ère instance de Errachidia', 'المحكمة الابتدائية - الرشيدية', true),
('TR_PIN_73', 'Tribunal de 1ère instance de Midelt', 'المحكمة الابتدائية - ميدلت', true),
('TR_PIN_74', 'Tribunal de 1ère instance de Nador', 'المحكمة الابتدائية - الناظور', true),
('TR_PIN_75', 'Tribunal de 1ère instance de Driouch', 'المحكمة الابتدائية - الدريوش', true),
('TR_PIN_76', 'Tribunal de 1ère instance de Fes', 'المحكمة الابتدائية - فاس', true),
('TR_PIN_77', 'Tribunal de 1ère instance de Boulemane', 'المحكمة الابتدائية - بولمان', true),
('TR_PIN_78', 'Tribunal de 1ère instance de Sefrou', 'المحكمة الابتدائية - صفرو', true),
('TR_PIN_79', 'Tribunal de 1ère instance de Taouante', 'المحكمة الابتدائية - تاونات', true),
('TR_PIN_80', 'Tribunal de 1ère instance de Guelmim', 'المحكمة الابتدائية كلميم', true),
('TR_PIN_81', 'Tribunal de 1ère instance de Assa Zag', 'المحكمة الابتدائية أسا - الزاك', true),
('TR_PIN_82', 'Tribunal de 1ère instance de Tantan', 'المحكمة الابتدائية طانطان', true),
('TR_PIN_83', 'Tribunal de 1ère instance de Sidi Ifni', 'المحكمة الابتدائية سيدي إفني', true),
('TR_CASS_1', 'Cour de cassation de Rabat', 'محكمة النقض بالرباط', true)
ON CONFLICT (code) DO UPDATE SET
    name_fr = EXCLUDED.name_fr,
    name_ar = EXCLUDED.name_ar,
    active = EXCLUDED.active;
```

**Step 2: Verify**
```bash
mvn flyway:migrate -pl backend
# Expect: Successfully applied migration. psql: SELECT COUNT(*) FROM tribunals; → 125+
```

---

## Task 3 — V58: Replace case type seeds (4 types from Excel)

**Files:**
- Create: `backend/src/main/resources/db/migration/V58__replace_case_type_seeds.sql`

**Step 1: Write migration**

The Excel defines 4 case natures: CIVIL, PENAL, COMMERCIAL, ADMINISTRATIVE. The `number_format_template` pattern follows the same convention as existing types. Use UPSERT — existing cases reference these via FK so do not DELETE.

```sql
-- V58__replace_case_type_seeds.sql
INSERT INTO case_types (code, name_fr, name_ar, number_format_template, active) VALUES
('CIVIL',           'Civile',          'مدني',   '{year}/{tribunalCode}/{typeCode}/{sequence:04d}', true),
('PENAL',           'Pénale',          'جنائي',  '{year}/{tribunalCode}/{typeCode}/{sequence:04d}', true),
('COMMERCIAL',      'Commerciale',     'تجاري',  '{year}/{tribunalCode}/{typeCode}/{sequence:04d}', true),
('ADMINISTRATIVE',  'Administrative',  'إداري',  '{year}/{tribunalCode}/{typeCode}/{sequence:04d}', true)
ON CONFLICT (code) DO UPDATE SET
    name_fr = EXCLUDED.name_fr,
    name_ar = EXCLUDED.name_ar,
    active = EXCLUDED.active;
```

> **Note:** If the existing code for commercial cases is `COMMERC` (not `COMMERCIAL`), check the current seed in V20 first and align accordingly. Run `SELECT code FROM case_types;` before applying this migration.

**Step 2: Verify**
```bash
mvn flyway:migrate -pl backend
# psql: SELECT code, name_fr FROM case_types; → 4 rows
```

---

## Task 4 — V59: Seed 426 case categories from Excel

**Files:**
- Create: `backend/src/main/resources/db/migration/V59__replace_case_category_seeds.sql`

**Step 1: Write migration**

Categories link to `case_types.id` via FK. Use a CTE to look up IDs by code. Use `ON CONFLICT (code) DO NOTHING` because duplicate codes exist in the Excel source (same numeric code, different description — only the first one wins).

The `name_fr` column receives the Arabic name as placeholder (no French translations available in source data).

```sql
-- V59__replace_case_category_seeds.sql
-- Seeds 426 Moroccan court case category codes.
-- Source: VersementAvocatsRAF Excel, sheet "Code Numéro dossier".
-- name_fr is intentionally set to the Arabic name as placeholder.
-- Duplicate codes are skipped via ON CONFLICT DO NOTHING.

WITH type_ids AS (
    SELECT code AS type_code, id AS type_id FROM case_types
),
cats (code, name_ar, type_code) AS (
    VALUES
    -- ADMINISTRATIVE (7xxx)
    ('7101', 'القضايا الاستعجالية', 'ADMINISTRATIVE'),
    ('7102', 'الأوامر المبنية على طلب', 'ADMINISTRATIVE'),
    ('7103', 'المصادقة على الحجز', 'ADMINISTRATIVE'),
    ('7104', 'المعاشات المدنية والعسكرية', 'ADMINISTRATIVE'),
    ('7105', 'الوضعية الفردية', 'ADMINISTRATIVE'),
    ('7106', 'إيقاف التنفيذ', 'ADMINISTRATIVE'),
    ('7107', 'المنازعات الإنتخابية', 'ADMINISTRATIVE'),
    ('7108', 'نزع الملكية', 'ADMINISTRATIVE'),
    ('7109', 'تحصيل ديون الخزينة', 'ADMINISTRATIVE'),
    ('7110', 'دعوى الإلغاء', 'ADMINISTRATIVE'),
    ('7111', 'فحص الشرعية', 'ADMINISTRATIVE'),
    ('7112', 'المسؤولية الإدارية', 'ADMINISTRATIVE'),
    ('7113', 'المنازعات الضريبية في الموضوع', 'ADMINISTRATIVE'),
    ('7114', 'العقود الإدارية والصفقات', 'ADMINISTRATIVE'),
    ('7115', 'المختلفة', 'ADMINISTRATIVE'),
    ('7116', 'المساعدة القضائية', 'ADMINISTRATIVE'),
    ('7129', 'الوضعية الفردية المادة 11 من ظ 10 - 09 - 1993', 'ADMINISTRATIVE'),
    ('7130', 'المنازعات المادة 11 من ظ 10 - 09 - 1993', 'ADMINISTRATIVE'),
    ('7201', 'الاستعجالي', 'ADMINISTRATIVE'),
    ('7202', 'الاستعجالي مستأنف', 'ADMINISTRATIVE'),
    ('7203', 'غرفة المشورة', 'ADMINISTRATIVE'),
    ('7204', 'المساعدة القضائية', 'ADMINISTRATIVE'),
    ('7205', 'قضاء الإلغاء', 'ADMINISTRATIVE'),
    ('7206', 'المسؤولية الإدارية', 'ADMINISTRATIVE'),
    ('7207', 'العقود الإدارية والصفقات', 'ADMINISTRATIVE'),
    ('7208', 'الوضعية الفردية', 'ADMINISTRATIVE'),
    ('7209', 'المنازعات الضريبية في الموضوع', 'ADMINISTRATIVE'),
    ('7210', 'المعاشات المدنية والعسكرية', 'ADMINISTRATIVE'),
    ('7211', 'نزع الملكية', 'ADMINISTRATIVE'),
    ('7212', 'المنازعات الإنتخابية', 'ADMINISTRATIVE'),
    ('7213', 'تحصيل ديون الخزينة', 'ADMINISTRATIVE'),
    ('7214', 'إيقاف التنفيذ', 'ADMINISTRATIVE'),
    ('7215', 'فحص الشرعية', 'ADMINISTRATIVE'),
    ('7301', 'التبليغ', 'ADMINISTRATIVE'),
    ('7302', 'الإنابات الواردة تبليغ', 'ADMINISTRATIVE'),
    ('7401', 'التنفيذ', 'ADMINISTRATIVE'),
    ('7402', 'الإنابات الواردة تنفيذ', 'ADMINISTRATIVE'),
    ('7501', 'التبليغ بناء على طلب', 'ADMINISTRATIVE'),
    ('7502', 'تبليغ تلقائي', 'ADMINISTRATIVE'),
    ('7503', 'الإنابات الواردة تبليغ', 'ADMINISTRATIVE'),
    ('7601', 'التنفيذ', 'ADMINISTRATIVE'),
    ('7602', 'الإنابات الواردة تنفيذ', 'ADMINISTRATIVE'),
    ('7603', 'الإنابات الصادرة تنفيذ', 'ADMINISTRATIVE'),
    -- COMMERCIAL (8xxx)
    ('8101', 'الاستعجالي', 'COMMERCIAL'),
    ('8102', 'الأمر بالأداء', 'COMMERCIAL'),
    ('8103', 'الأوامر المبنية على طلب', 'COMMERCIAL'),
    ('8104', 'استرجاع السيارات', 'COMMERCIAL'),
    ('8105', 'الحجز لدى الغير', 'COMMERCIAL'),
    ('8106', 'الحجز التحفظي', 'COMMERCIAL'),
    ('8107', 'رفع الحجز', 'COMMERCIAL'),
    ('8108', 'مسطرة التوفيق', 'COMMERCIAL'),
    ('8109', 'إيقاف التنفيذ', 'COMMERCIAL'),
    ('8110', 'صعوبة التنفيذ', 'COMMERCIAL'),
    ('8111', 'التوزيع الودي', 'COMMERCIAL'),
    ('8112', 'التسوية الودية', 'COMMERCIAL'),
    ('8201', 'العقود التجارية', 'COMMERCIAL'),
    ('8202', 'المنازعات بين التجار بشأن أعمال تجارية', 'COMMERCIAL'),
    ('8203', 'الأوراق التجارية', 'COMMERCIAL'),
    ('8204', 'منازعات شركاء في شركة تجارية', 'COMMERCIAL'),
    ('8205', 'الأصول التجارية', 'COMMERCIAL'),
    ('8206', 'منازعات ظهير 24 ماي 1955', 'COMMERCIAL'),
    ('8207', 'أكرية المحلات التجارية', 'COMMERCIAL'),
    ('8208', 'تصحيح الحجز', 'COMMERCIAL'),
    ('8209', 'معاملات مؤسسات التمويل', 'COMMERCIAL'),
    ('8210', 'المعاملات البنكية والقروض', 'COMMERCIAL'),
    ('8211', 'المنافسة غير المشروعة والتقليد', 'COMMERCIAL'),
    ('8212', 'مشورة متنوعة', 'COMMERCIAL'),
    ('8213', 'التنفيذ', 'COMMERCIAL'),
    ('8214', 'منازعات السجل التجاري', 'COMMERCIAL'),
    ('8215', 'غرامات السجل التجاري', 'COMMERCIAL'),
    ('8216', 'التعرض على أمر بالأداء', 'COMMERCIAL'),
    ('8217', 'إيقاف تنفيذ أمر بالأداء', 'COMMERCIAL'),
    ('8218', 'قضايا التأمين', 'COMMERCIAL'),
    ('8220', 'المسؤولية البنكية', 'COMMERCIAL'),
    ('8221', 'المعاملات البنكية', 'COMMERCIAL'),
    ('8222', 'القروض البنكية', 'COMMERCIAL'),
    ('8223', 'الأوامر بالأداء', 'COMMERCIAL'),
    ('8224', 'الأوامر المبنية على طلب', 'COMMERCIAL'),
    ('8225', 'الأوامر الاستعجالية', 'COMMERCIAL'),
    ('8226', 'الحجز لدى الغير', 'COMMERCIAL'),
    ('8227', 'الاختصاص', 'COMMERCIAL'),
    ('8228', 'المنازعات الناشئة بين الشركات التجارية', 'COMMERCIAL'),
    ('8229', 'الطعن في مقررات مكتب الملكية الصناعية', 'COMMERCIAL'),
    ('8230', 'الطعن بالبطلان في الأحكام التحكيمية', 'COMMERCIAL'),
    ('8231', 'إصلاح خطأ مادي', 'COMMERCIAL'),
    ('8232', 'قضايا أخرى', 'COMMERCIAL'),
    ('8301', 'صعوبات المقاولة', 'COMMERCIAL'),
    ('8302', 'التسوية القضائية', 'COMMERCIAL'),
    ('8303', 'التصفية القضائية', 'COMMERCIAL'),
    ('8304', 'الطلبات الأخرى المعروضة على القاضي المنتدب', 'COMMERCIAL'),
    ('8305', 'تمديد فترة إعداد الحل', 'COMMERCIAL'),
    ('8306', 'إعداد الحل', 'COMMERCIAL'),
    ('8307', 'تحديد الأتعاب', 'COMMERCIAL'),
    ('8308', 'فسخ المخطط', 'COMMERCIAL'),
    ('8309', 'متابعة تنفيذ المخطط', 'COMMERCIAL'),
    ('8310', 'العقوبات', 'COMMERCIAL'),
    ('8311', 'الإستبدال', 'COMMERCIAL'),
    ('8312', 'قفل المسطرة', 'COMMERCIAL'),
    ('8313', 'طلبات تحقيق الدين المعروضة على القاضي المنتدب', 'COMMERCIAL'),
    ('8314', 'تمديد فترة الاستغلال', 'COMMERCIAL'),
    ('8315', 'مسطرة الإنقاذ', 'COMMERCIAL'),
    ('8316', 'حصر مخطط الإستمرارية', 'COMMERCIAL'),
    ('8317', 'اختيار عرض التفويت', 'COMMERCIAL'),
    ('8318', 'تحويل التسوية إلى تصفية', 'COMMERCIAL'),
    ('8319', 'طلبات أخرى في صعوبات المقاولة', 'COMMERCIAL'),
    ('8320', 'تمديد التسوية القضائية', 'COMMERCIAL'),
    ('8321', 'تمديد التصفية القضائية', 'COMMERCIAL'),
    ('8322', 'قفل مسطرة التسوية القضائية', 'COMMERCIAL'),
    ('8323', 'قفل مسطرة التصفية القضائية', 'COMMERCIAL'),
    ('8401', 'تبليغ الأحكام والأوامر والقرارات محلي', 'COMMERCIAL'),
    ('8402', 'تبليغ الأحكام والأوامر والقرارات إنابة', 'COMMERCIAL'),
    ('8403', 'تبليغ أوامر القاضي المنتدب محلي', 'COMMERCIAL'),
    ('8404', 'تبليغ أوامر القاضي المنتدب إنابة', 'COMMERCIAL'),
    ('8405', 'تبليغ أحكام غرفة المشورة', 'COMMERCIAL'),
    ('8406', 'تبليغ تلقائي', 'COMMERCIAL'),
    ('8407', 'تبليغات أخرى', 'COMMERCIAL'),
    ('8408', 'تبليغ الأوامر الاستعجالية', 'COMMERCIAL'),
    ('8409', 'تبليغ الأوامر بالأداء', 'COMMERCIAL'),
    ('8410', 'تبليغ الإنذارات', 'COMMERCIAL'),
    ('8411', 'تبليغ أحكام الموضوع', 'COMMERCIAL'),
    ('8412', 'تبليغ أحكام صعوبات المقاولة', 'COMMERCIAL'),
    ('8501', 'الأداءات محلي', 'COMMERCIAL'),
    ('8502', 'الإفراغات محلي', 'COMMERCIAL'),
    ('8503', 'الإسترجاعات محلي', 'COMMERCIAL'),
    ('8504', 'حجز تحفظي على منقول محلي', 'COMMERCIAL'),
    ('8505', 'حجز تحفظي على عقار محلي', 'COMMERCIAL'),
    ('8506', 'حجز لدى الغير محلي', 'COMMERCIAL'),
    ('8507', 'تنفيذ الأوامر الإستعجالية محلي', 'COMMERCIAL'),
    ('8508', 'حجز تنفيذي على منقول محلي', 'COMMERCIAL'),
    ('8509', 'تنفيذ الإسترجاعات محلي', 'COMMERCIAL'),
    ('8510', 'تنفيذ الأوامر بالأداء محلي', 'COMMERCIAL'),
    ('8511', 'تنفيذ الأداءات محلي', 'COMMERCIAL'),
    ('8512', 'تحقيق الرهن على أصل تجاري محلي', 'COMMERCIAL'),
    ('8513', 'حجز تنفيذي على أصل تجاري محلي', 'COMMERCIAL'),
    ('8514', 'حجز لدى الغير محلي تنفيذ', 'COMMERCIAL'),
    ('8515', 'حجز تنفيذي على عقار محلي', 'COMMERCIAL'),
    ('8516', 'البيوعات المتعلقة بصعوبات المقاولة محلي', 'COMMERCIAL'),
    ('8517', 'حجز تنفيذي على باخرة محلي', 'COMMERCIAL'),
    ('8518', 'البيوعات المتعلقة بصعوبات المقاولة', 'COMMERCIAL'),
    ('8519', 'تنفيذات أخرى محلي', 'COMMERCIAL'),
    ('8521', 'الأداءات إنابة', 'COMMERCIAL'),
    ('8522', 'الإفراغات إنابة', 'COMMERCIAL'),
    ('8523', 'الإسترجاعات إنابة', 'COMMERCIAL'),
    ('8524', 'حجز تحفظي على منقول إنابة', 'COMMERCIAL'),
    ('8525', 'حجز تحفظي على أصل تجاري إنابة', 'COMMERCIAL'),
    ('8526', 'حجز تحفظي على باخرة إنابة', 'COMMERCIAL'),
    ('8527', 'حجز تحفظي على عقار إنابة', 'COMMERCIAL'),
    ('8528', 'حجز تنفيذي على منقول إنابة', 'COMMERCIAL'),
    ('8529', 'حجز تنفيذي على أصل تجاري إنابة', 'COMMERCIAL'),
    ('8530', 'حجز تنفيذي على باخرة إنابة', 'COMMERCIAL'),
    ('8531', 'حجز تنفيذي على عقار إنابة', 'COMMERCIAL'),
    ('8532', 'تحقيق الرهن على أصل تجاري إنابة', 'COMMERCIAL'),
    ('8533', 'تحقيق الرهن على عقار إنابة', 'COMMERCIAL'),
    ('8534', 'حجز لدى الغير إنابة', 'COMMERCIAL'),
    ('8535', 'البيوعات إنابة', 'COMMERCIAL'),
    ('8536', 'البيوعات المتعلقة بصعوبات المقاولة إنابة', 'COMMERCIAL'),
    ('8537', 'رفع الحجز إنابة', 'COMMERCIAL'),
    ('8538', 'تنفيذات أخرى إنابة', 'COMMERCIAL'),
    -- PENAL (2xxx)
    ('2101', 'جنحي عادي تأديبي', 'PENAL'),
    ('2102', 'جنحي عادي ضبطي', 'PENAL'),
    ('2103', 'جنحي تلبسي اعتقال', 'PENAL'),
    ('2104', 'جنحي تلبسي سراح', 'PENAL'),
    ('2105', 'جنحي ضبطي اعتقال', 'PENAL'),
    ('2106', 'جنحي ضبطي سراح', 'PENAL'),
    ('2801', 'جنحي عادي استئنافي', 'PENAL'),
    ('2802', 'جنحي تلبسي استئنافي اعتقال', 'PENAL'),
    ('2803', 'جنحي تلبسي استئنافي سراح', 'PENAL'),
    -- CIVIL (1xxx and 6xxx enforcement)
    ('1101', 'الاستعجالي', 'CIVIL'),
    ('1102', 'الأمر بالأداء', 'CIVIL'),
    ('1103', 'الحجز التحفظي', 'CIVIL'),
    ('1104', 'الحجز لدى الغير', 'CIVIL'),
    ('1105', 'التقييد الاحتياطي', 'CIVIL'),
    ('1106', 'الإنذار العقاري', 'CIVIL'),
    ('1107', 'الإنذار بأداء الكراء', 'CIVIL'),
    ('1108', 'الإحالة', 'CIVIL'),
    ('1109', 'باقي الأوامر المبنية على طلب', 'CIVIL'),
    ('1110', 'تبليغ إنذار ظ.ه. 64 - 99', 'CIVIL'),
    ('1111', 'المصادقة على الإنذار ظ.ه. 64 - 99', 'CIVIL'),
    ('1112', 'المصادقة على الحجز', 'CIVIL'),
    ('1113', 'الإلغاء ق ق', 'CIVIL'),
    ('1114', 'طلبات الإستفادة من صندوق التكافل العائلي', 'CIVIL'),
    ('1120', 'الطعن ضد مقررات اتعاب هيئة المحامين', 'CIVIL'),
    ('1121', 'صعوبة التنفيذ', 'CIVIL'),
    ('1122', 'اداء اليميين المهنية', 'CIVIL'),
    ('1123', 'إيقاف التنفيذ', 'CIVIL'),
    ('1124', 'الطعون ضد مقررات مجلس هيئة المحامين', 'CIVIL'),
    ('1154', 'المصادقة على الحجز لدى الغير', 'CIVIL'),
    ('1201', 'المدني المتنوع', 'CIVIL'),
    ('1202', 'المسؤولية التقصيرية', 'CIVIL'),
    ('1203', 'التجاري', 'CIVIL'),
    ('1204', 'الإداري', 'CIVIL'),
    ('1205', 'المدني النهائي', 'CIVIL'),
    ('1207', 'تذييل بالصيغة التنفيذية', 'CIVIL'),
    ('1211', 'المنازعات الإنتخابية', 'CIVIL'),
    ('1220', 'الأوامر بالأداء', 'CIVIL'),
    ('1221', 'القضايا الاستعجالية', 'CIVIL'),
    ('1301', 'أداء واجبات الكراء', 'CIVIL'),
    ('1302', 'الإفراغ', 'CIVIL'),
    ('1303', 'الأداء والإفراغ', 'CIVIL'),
    ('1304', 'مراجعة السومة الكرائية', 'CIVIL'),
    ('1401', 'الاستعجالي', 'CIVIL'),
    ('1402', 'الاستعجالي استئناف', 'CIVIL'),
    ('1403', 'العقار في طور التحفيظ', 'CIVIL'),
    ('1404', 'القضايا العقارية العينية المختلطة', 'CIVIL'),
    ('1501', 'نزاعات الشغل', 'CIVIL'),
    ('1502', 'حوادث الشغل', 'CIVIL'),
    ('1503', 'الأمراض المهنية', 'CIVIL'),
    ('1601', 'الإصلاح والتغيير', 'CIVIL'),
    ('1602', 'تسجيل الولادة', 'CIVIL'),
    ('1603', 'تسجيل الوفاة', 'CIVIL'),
    ('1605', 'الزواج عادي', 'CIVIL'),
    ('1606', 'النفقة', 'CIVIL'),
    ('1607', 'التطليق', 'CIVIL'),
    ('1608', 'الطلاق', 'CIVIL'),
    ('1609', 'الحضانة', 'CIVIL'),
    ('1610', 'الرجوع لبيت الزوجية', 'CIVIL'),
    ('1611', 'ثبوت الزوجية', 'CIVIL'),
    ('1612', 'صلة الرحم', 'CIVIL'),
    ('1613', 'النسب', 'CIVIL'),
    ('1614', 'التذييل بالصيغة التنفيذية أسرة', 'CIVIL'),
    ('1615', 'الميراث', 'CIVIL'),
    ('1616', 'زواج القاصرين', 'CIVIL'),
    ('1617', 'كفالة الأطفال المهملين', 'CIVIL'),
    ('1618', 'الإذن بالتعدد', 'CIVIL'),
    ('1619', 'النيابة الشرعية', 'CIVIL'),
    ('1620', 'قضايا الأحوال الشخصية الأخرى', 'CIVIL'),
    ('1622', 'مراجعة لوازم الطلاق', 'CIVIL'),
    ('1623', 'التحجير', 'CIVIL'),
    ('1624', 'الزواج المختلط', 'CIVIL'),
    ('1625', 'التصريح بالإهمال', 'CIVIL'),
    ('1626', 'التطليق للشقاق', 'CIVIL'),
    ('1627', 'التطليق للضرر', 'CIVIL'),
    ('1628', 'التطليق لعدم الإنفاق', 'CIVIL'),
    ('1629', 'التطليق للغيبة', 'CIVIL'),
    ('1630', 'التطليق للعيب', 'CIVIL'),
    ('1631', 'التطليق بسبب الإيلاء والهجر', 'CIVIL'),
    ('1632', 'التطليق لإخلال الزوج بشرط من شروط العقد', 'CIVIL'),
    ('1633', 'الطلاق قبل البناء', 'CIVIL'),
    ('1634', 'الطلاق المملك', 'CIVIL'),
    ('1635', 'الطلاق الإتفاقي', 'CIVIL'),
    ('1636', 'الطلاق بالخلع', 'CIVIL'),
    ('1637', 'فسخ عقد زواج', 'CIVIL'),
    ('1638', 'بطلان الزواج', 'CIVIL'),
    ('1641', 'دعاوى الجنسية', 'CIVIL'),
    ('1642', 'كفالة الأطفال غير المهملين', 'CIVIL'),
    ('1643', 'النيابة القانونية', 'CIVIL'),
    ('1701', 'طلب الأداء ق ق', 'CIVIL'),
    ('1702', 'الحجز لدى الغير ق ق', 'CIVIL'),
    ('1703', 'الإنذار بأداء الكراء ق ق', 'CIVIL'),
    -- Civil enforcement (6xxx)
    ('6101', 'البيوعات العقارية', 'CIVIL'),
    ('6102', 'الحجز التحفظي على عقار', 'CIVIL'),
    ('6103', 'بيع الأصول التجارية', 'CIVIL'),
    ('6104', 'التصفية القضائية', 'CIVIL'),
    ('6105', 'الانذارات العقارية', 'CIVIL'),
    ('6106', 'التركات الشاغرة', 'CIVIL'),
    ('6107', 'الحجز التنفيذي على عقار', 'CIVIL'),
    ('6108', 'تحقيق الرهن على عقار', 'CIVIL'),
    ('6109', 'الحجز التحفظي على أصل تجاري', 'CIVIL'),
    ('6110', 'تحقيق الرهن على أصل تجاري', 'CIVIL'),
    ('6150', 'تنفيذ الأمر بالأداء', 'CIVIL'),
    ('6151', 'تنفيذ الأوامر المختلفة', 'CIVIL'),
    ('6152', 'تنفيذ الأوامر الإستعجالية', 'CIVIL'),
    ('6201', 'مختلف قضايا التنفيذ المدني', 'CIVIL'),
    ('6202', 'الحجز التحفظي على منقول', 'CIVIL'),
    ('6203', 'الحجز لدى الغير', 'CIVIL'),
    ('6204', 'الإفراغات', 'CIVIL'),
    ('6205', 'الحجز التنفيذي على منقول', 'CIVIL'),
    ('6206', 'تنفيذ الأداءات', 'CIVIL'),
    ('6207', 'تنفيذ الإسترجاعات', 'CIVIL'),
    ('6208', 'تنفيذات المسؤولية المدنية', 'CIVIL'),
    ('6209', 'تنفيذات حوادث السير الدعوى المدنية', 'CIVIL'),
    ('6210', 'تنفيذات منازعات الشغل', 'CIVIL'),
    ('6211', 'تنفيذات حوادث الشغل', 'CIVIL'),
    ('6250', 'تنفيذ النفقة', 'CIVIL'),
    ('6251', 'تنفيذ الرجوع لبيت الزوجية', 'CIVIL'),
    ('6252', 'تنفيذ مستحقات الطلاق', 'CIVIL'),
    ('6253', 'تنفيذ مستحقات التطليق', 'CIVIL'),
    ('6301', 'الانابات الصادرة عقار', 'CIVIL'),
    ('6302', 'الانابات الواردة عقار', 'CIVIL'),
    ('6303', 'الانابات الصادرة تنفيذ مدني', 'CIVIL'),
    ('6304', 'الانابات الواردة تنفيذ مدني', 'CIVIL'),
    ('6501', 'تبليغات الإنذارات', 'CIVIL'),
    ('6502', 'تبليغات الأوامر بالأداء', 'CIVIL'),
    ('6503', 'تبليغ ملفات أوامر الرئيس', 'CIVIL'),
    ('6504', 'تبليغات الأوامر الإستعجالية', 'CIVIL'),
    ('6505', 'تبليغات الحجز لدى الغير', 'CIVIL'),
    ('6601', 'تنفيذ الغرامات المالية', 'CIVIL'),
    ('6602', 'تنفيذ العقوبات السالبة للحرية', 'CIVIL'),
    ('6603', 'إنابة واردة الغرامات المالية', 'CIVIL'),
    ('6701', 'تبليغات المسؤولية التقصيرية', 'CIVIL'),
    ('6702', 'تبليغات الدعوى المدنية حوادث السير', 'CIVIL'),
    ('6801', 'تبليغات الحجز لدى الغير إنابات واردة', 'CIVIL'),
    ('6820', 'تبليغات النفقة إنابات واردة', 'CIVIL'),
    ('6821', 'تبليغات الطلاق إنابات واردة', 'CIVIL')
)
INSERT INTO case_categories (code, name_fr, name_ar, case_type_id, active)
SELECT c.code, c.name_ar, c.name_ar, t.type_id, true
FROM cats c
JOIN type_ids t ON c.type_code = t.type_code
ON CONFLICT (code) DO NOTHING;
```

**Step 2: Verify**
```bash
mvn flyway:migrate -pl backend
# psql: SELECT COUNT(*) FROM case_categories; → significantly higher count
```

---

## Task 5 — V60: Seed financial transactions (impl2)

**Files:**
- Create: `backend/src/main/resources/db/migration/V60__seed_financial_transactions.sql`

**Step 1: Write migration**

Seeds synthetic REVENUE transactions linked to the first seeded test case. The VAA sheet provides real 2024 payment data (dates as Excel serials converted to ISO dates below).

```sql
-- V60__seed_financial_transactions.sql
-- Seeds 11 representative financial transactions for development/testing.
-- All linked to the first test case seeded in V33.
-- Dates sourced from VAA sheet (Excel serial → ISO date conversion applied).

INSERT INTO financial_transactions
    (case_id, direction, operation_type, payment_mode, amount, payment_date, description, created_at, updated_at)
SELECT
    c.id,
    t.direction,
    t.operation_type,
    t.payment_mode,
    t.amount,
    t.payment_date,
    t.description,
    NOW(),
    NOW()
FROM (
    VALUES
    ('REVENUE'::text, 'OPENING_FEE'::text,    'TRANSFER'::text, 1500.00::numeric, '2024-03-26'::date, 'Honoraires affaire pénale - Tribunal d''appel Rabat'),
    ('REVENUE',       'OPENING_FEE',           'TRANSFER',       1500.00,          '2024-06-15'::date, 'Honoraires affaire pénale - Tribunal d''appel Rabat'),
    ('REVENUE',       'PROCEDURE_FEE',         'CHECK',          800.00,           '2024-02-16'::date, 'Frais procédure civile - استعجالي'),
    ('REVENUE',       'PROCEDURE_FEE',         'CASH',           600.00,           '2024-12-05'::date, 'Frais affaire civile - الأداء والإفراغ Rabat'),
    ('REVENUE',       'OPENING_FEE',           'TRANSFER',       1500.00,          '2024-03-10'::date, 'Honoraires affaire pénale استئنافي'),
    ('REVENUE',       'PROCEDURE_FEE',         'TRANSFER',       700.00,           '2024-04-22'::date, 'Frais procédure استعجالي - Rabat 1ère instance'),
    ('REVENUE',       'INTERVENTION_FEE',      'CHECK',          900.00,           '2024-11-28'::date, 'Intervention عقار في طور التحفيظ'),
    ('REVENUE',       'DOCUMENT_FEE',          'CASH',           350.00,           '2024-05-05'::date, 'Documents التذييل بالصيغة التنفيذية أسرة'),
    ('REVENUE',       'PROCEDURE_FEE',         'TRANSFER',       600.00,           '2024-08-06'::date, 'Frais الأداء والإفراغ - Casablanca Appel'),
    ('REVENUE',       'OPENING_FEE',           'TRANSFER',       1500.00,          '2024-06-28'::date, 'Honoraires جنحي عادي استئنافي - Rabat Appel'),
    ('EXPENSE',       'JUDICIAL_TAX',          'TRANSFER',       200.00,           '2024-03-19'::date, 'Taxe judiciaire رسوم المحكمة')
) AS t(direction, operation_type, payment_mode, amount, payment_date, description)
CROSS JOIN (SELECT id FROM cases WHERE deleted_at IS NULL ORDER BY id LIMIT 1) AS c;
```

**Step 2: Verify**
```bash
mvn flyway:migrate -pl backend
# psql: SELECT COUNT(*) FROM financial_transactions; → 11+
```

---

## Task 6 — V61: Add before/after snapshot columns to audit_logs

**Files:**
- Create: `backend/src/main/resources/db/migration/V61__add_audit_snapshot_columns.sql`

**Step 1: Write migration**

```sql
-- V61__add_audit_snapshot_columns.sql
ALTER TABLE audit_logs
    ADD COLUMN old_values TEXT,
    ADD COLUMN new_values TEXT;

COMMENT ON COLUMN audit_logs.old_values IS 'JSON snapshot of entity state before the action';
COMMENT ON COLUMN audit_logs.new_values IS 'JSON snapshot of entity state after the action';
```

**Step 2: Verify**
```bash
mvn flyway:migrate -pl backend
# psql: \d audit_logs → old_values and new_values columns present
```

---

## Task 7 — V62: Wire allowed statuses for new case types

**Files:**
- Create: `backend/src/main/resources/db/migration/V62__wire_case_type_statuses_for_administrative.sql`

**Step 1: Write migration**

```sql
-- V62__wire_case_type_statuses_for_administrative.sql
-- Add all standard statuses (DRAFT, ACTIVE, CLOSED, ARCHIVED, ADJOURNED) to ADMINISTRATIVE case type.
-- Other types already have their statuses from V26.

INSERT INTO case_type_statuses (case_type_id, case_status_id)
SELECT ct.id, cs.id
FROM case_types ct
CROSS JOIN case_statuses cs
WHERE ct.code = 'ADMINISTRATIVE'
ON CONFLICT DO NOTHING;
```

**Step 2: Verify**
```bash
mvn flyway:migrate -pl backend
# psql: SELECT COUNT(*) FROM case_type_statuses WHERE case_type_id = (SELECT id FROM case_types WHERE code='ADMINISTRATIVE');
# → 5 rows (one per status)
```

---

## Task 8 — Backend: Invoice ↔ Transaction Sync (bug4)

**Files:**
- Modify: `backend/src/main/java/com/lawfirm/domain/model/FinancialTransaction.java`
- Modify: `backend/src/main/java/com/lawfirm/application/dto/request/InvoiceStatusRequest.java`
- Modify: `backend/src/main/java/com/lawfirm/application/dto/response/FinancialTransactionResponse.java`
- Modify: `backend/src/main/java/com/lawfirm/application/mapper/FinancialTransactionMapper.java`
- Modify: `backend/src/main/java/com/lawfirm/application/service/InvoiceService.java`

**Step 1: Add invoice FK to FinancialTransaction entity**

In `FinancialTransaction.java`, add after existing fields (before `deletedAt`):
```java
@ManyToOne(fetch = FetchType.LAZY)
@JoinColumn(name = "invoice_id")
private Invoice invoice;
```

**Step 2: Extend InvoiceStatusRequest**

Replace the entire `InvoiceStatusRequest.java`:
```java
package com.lawfirm.application.dto.request;

import com.lawfirm.domain.model.FinancialTransaction;
import com.lawfirm.domain.model.Invoice;
import jakarta.validation.constraints.NotNull;
import java.time.LocalDate;

public record InvoiceStatusRequest(
    @NotNull Invoice.InvoiceStatus status,
    FinancialTransaction.PaymentMode paymentMode,   // required when status = PAID
    LocalDate paymentDate,                           // required when status = PAID
    String paymentReference                          // optional
) {}
```

**Step 3: Add invoiceId to FinancialTransactionResponse**

In `FinancialTransactionResponse.java`, add `invoiceId` field:
```java
public record FinancialTransactionResponse(
    Long id,
    Long caseId,
    String caseNumber,
    Long invoiceId,           // ← NEW: null when created manually
    FinancialTransaction.Direction direction,
    FinancialTransaction.OperationType operationType,
    FinancialTransaction.PaymentMode paymentMode,
    BigDecimal amount,
    LocalDate paymentDate,
    String paymentReference,
    String accountNumber,
    String description,
    LocalDateTime createdAt
) {}
```

**Step 4: Update FinancialTransactionMapper**

In `FinancialTransactionMapper.java`, add invoice mapping:
```java
@Mapper(componentModel = "spring")
public interface FinancialTransactionMapper {
    @Mapping(target = "caseId", source = "caseEntity.id")
    @Mapping(target = "caseNumber", source = "caseEntity.fullCaseNumber")
    @Mapping(target = "invoiceId", source = "invoice.id")
    FinancialTransactionResponse toResponse(FinancialTransaction transaction);
}
```

**Step 5: Extend InvoiceService.updateStatus()**

Inject `FinancialTransactionRepository` into `InvoiceService`. Then replace `updateStatus()`:
```java
@Transactional
public InvoiceResponse updateStatus(Long id, InvoiceStatusRequest request) {
    Invoice invoice = invoiceRepository.findByIdAndDeletedAtIsNull(id)
        .orElseThrow(() -> new ResourceNotFoundException("Invoice", id));

    // Validate transition
    Invoice.InvoiceStatus current = invoice.getStatus();
    Invoice.InvoiceStatus next = request.status();
    if (!isValidTransition(current, next)) {
        throw new IllegalStateException(
            "Cannot transition invoice from " + current + " to " + next);
    }

    // If marking PAID, validate payment fields and auto-create transaction
    if (next == Invoice.InvoiceStatus.PAID) {
        if (request.paymentMode() == null || request.paymentDate() == null) {
            throw new IllegalArgumentException(
                "paymentMode and paymentDate are required when marking invoice as PAID");
        }
        FinancialTransaction tx = new FinancialTransaction();
        tx.setCaseEntity(invoice.getCaseEntity());
        tx.setInvoice(invoice);
        tx.setDirection(FinancialTransaction.Direction.REVENUE);
        tx.setOperationType(FinancialTransaction.OperationType.OTHER);
        tx.setPaymentMode(request.paymentMode());
        tx.setAmount(invoice.getTotalAmount());
        tx.setPaymentDate(request.paymentDate());
        tx.setPaymentReference(request.paymentReference());
        tx.setDescription("Paiement facture #" + invoice.getInvoiceNumber());
        financialTransactionRepository.save(tx);
    }

    invoice.setStatus(next);
    return invoiceMapper.toResponse(invoiceRepository.save(invoice));
}

private boolean isValidTransition(Invoice.InvoiceStatus from, Invoice.InvoiceStatus to) {
    return switch (from) {
        case DRAFT -> to == Invoice.InvoiceStatus.SENT || to == Invoice.InvoiceStatus.CANCELLED;
        case SENT  -> to == Invoice.InvoiceStatus.PAID || to == Invoice.InvoiceStatus.CANCELLED;
        default    -> false; // PAID and CANCELLED are terminal
    };
}
```

**Step 6: Verify**
```bash
mvn clean compile -pl backend
# Expect: BUILD SUCCESS
```

---

## Task 9 — Backend: Searchable Reference Endpoints (bug1)

**Files:**
- Modify: `backend/src/main/java/com/lawfirm/domain/repository/TribunalRepository.java`
- Modify: `backend/src/main/java/com/lawfirm/domain/repository/CaseTypeRepository.java` (if exists)
- Modify: `backend/src/main/java/com/lawfirm/domain/repository/CaseCategoryRepository.java` (if exists)
- Modify: corresponding service files and controllers for Tribunal, CaseType, CaseCategory, Lawyer, Client

**Step 1: Add JpaSpecificationExecutor to repositories**

For each repository (example for Tribunal — repeat for CaseType, CaseCategory, Lawyer, Client):
```java
@Repository
public interface TribunalRepository extends JpaRepository<Tribunal, Long>,
        JpaSpecificationExecutor<Tribunal> {
    // ... existing methods remain unchanged
}
```

**Step 2: Add search method to each service**

Example for `TribunalService` (repeat pattern for all 5):
```java
public Page<TribunalResponse> search(String query, Pageable pageable) {
    Specification<Tribunal> spec = (root, q, cb) -> {
        if (query == null || query.isBlank()) {
            return cb.equal(root.get("active"), true);
        }
        String pattern = "%" + query.toLowerCase() + "%";
        return cb.and(
            cb.equal(root.get("active"), true),
            cb.or(
                cb.like(cb.lower(root.get("nameFr")), pattern),
                cb.like(cb.lower(root.get("nameAr")), pattern),
                cb.like(cb.lower(root.get("code")),   pattern)
            )
        );
    };
    return tribunalRepository.findAll(spec, pageable)
        .map(tribunalMapper::toResponse);
}
```

For `LawyerService` and `ClientService`, search on `firstName`, `lastName`, `email` (lawyers also `licenseNumber`).

For `CaseCategoryService`, also accept `caseTypeCode` filter alongside `search`.

**Step 3: Add @RequestParam to controllers**

Example for `TribunalController` (repeat for all 5 controllers):
```java
@GetMapping
@PreAuthorize("hasAuthority('CASE_READ')")
public ResponseEntity<Page<TribunalResponse>> list(
        @RequestParam(required = false) String search,
        @RequestParam(defaultValue = "0")  int page,
        @RequestParam(defaultValue = "20") int size) {
    Pageable pageable = PageRequest.of(page, size, Sort.by("nameFr"));
    return ResponseEntity.ok(tribunalService.search(search, pageable));
}
```

For `CaseCategoryController`:
```java
@GetMapping
public ResponseEntity<Page<CaseCategoryResponse>> list(
        @RequestParam(required = false) String search,
        @RequestParam(required = false) String caseTypeCode,
        @RequestParam(defaultValue = "0")  int page,
        @RequestParam(defaultValue = "20") int size) { ... }
```

**Step 4: Verify**
```bash
mvn clean compile -pl backend
curl "http://localhost:8080/api/tribunals?search=rabat&page=0&size=5" -H "Authorization: Bearer <token>"
# Expect: JSON page with tribunals whose name contains "rabat"
```

---

## Task 10 — Backend: Full Audit Snapshots (bug2)

**Files:**
- Modify: `backend/src/main/java/com/lawfirm/domain/model/AuditLog.java`
- Modify: `backend/src/main/java/com/lawfirm/application/service/AuditLogService.java`
- Modify: `backend/src/main/java/com/lawfirm/application/service/CaseService.java`
- Modify: `backend/src/main/java/com/lawfirm/application/dto/response/AuditLogResponse.java` (find or create)
- Modify: corresponding AuditLogMapper

**Step 1: Add fields to AuditLog entity**

In `AuditLog.java`, add after the `metadata` field:
```java
@Column(name = "old_values", columnDefinition = "TEXT")
private String oldValues;

@Column(name = "new_values", columnDefinition = "TEXT")
private String newValues;
```

**Step 2: Update AuditLogService**

Add `ObjectMapper` injection and a new overloaded `log()` method. Keep the existing one for backward compatibility:
```java
@Service
@RequiredArgsConstructor
public class AuditLogService {
    private final AuditLogRepository auditLogRepository;
    private final ObjectMapper objectMapper;

    // Existing method — keep unchanged for non-case audit calls
    public void log(String resource, Long resourceId, String action,
                    List<String> changedFields, String username) {
        AuditLog log = new AuditLog();
        log.setResource(resource);
        log.setResourceId(String.valueOf(resourceId));
        log.setAction(action);
        log.setUsername(username);
        log.setMetadata(String.join(",", changedFields));
        auditLogRepository.save(log);
    }

    // New overloaded method with before/after snapshots
    public void log(String resource, Long resourceId, String action,
                    Object oldValues, Object newValues, String username) {
        AuditLog log = new AuditLog();
        log.setResource(resource);
        log.setResourceId(String.valueOf(resourceId));
        log.setAction(action);
        log.setUsername(username);
        try {
            log.setOldValues(oldValues != null ? objectMapper.writeValueAsString(oldValues) : null);
            log.setNewValues(newValues != null ? objectMapper.writeValueAsString(newValues) : null);
        } catch (JsonProcessingException e) {
            log.setOldValues(null);
            log.setNewValues(null);
        }
        auditLogRepository.save(log);
    }
}
```

**Step 3: Update CaseService.updateCase()**

Inside the `updateCase()` method, before applying changes:
```java
// Capture state BEFORE changes
CaseResponse beforeSnapshot = caseMapper.toResponse(existingCase);

// ... existing update logic (apply tribunal, category, lawyers, etc.) ...

// Save and capture state AFTER changes
Case savedCase = caseRepository.save(existingCase);
CaseResponse afterSnapshot = caseMapper.toResponse(savedCase);

// Log with full snapshots (replaces old changedFields audit call)
auditLogService.log("CASE", id, "CASE_UPDATED", beforeSnapshot, afterSnapshot, getCurrentUsername());
```

**Step 4: Update AuditLogResponse DTO**

Add `oldValues` and `newValues` as `Map<String, Object>`:
```java
public record AuditLogResponse(
    Long id,
    String action,
    String resource,
    String resourceId,
    String username,
    String metadata,
    Map<String, Object> oldValues,   // ← NEW
    Map<String, Object> newValues,   // ← NEW
    LocalDateTime createdAt
) {}
```

**Step 5: Update AuditLogMapper**

```java
@Mapper(componentModel = "spring")
public interface AuditLogMapper {
    @Mapping(target = "oldValues", expression = "java(parseJson(log.getOldValues(), objectMapper))")
    @Mapping(target = "newValues", expression = "java(parseJson(log.getNewValues(), objectMapper))")
    AuditLogResponse toResponse(AuditLog log);

    default Map<String, Object> parseJson(String json, ObjectMapper objectMapper) {
        if (json == null) return null;
        try {
            return objectMapper.readValue(json, new TypeReference<Map<String, Object>>() {});
        } catch (Exception e) { return null; }
    }
}
```

> **Note:** MapStruct mappers cannot directly inject Spring beans. Either use `@Context ObjectMapper objectMapper` in the mapper, or move the JSON parsing into a `@AfterMapping` method in a decorator. The simplest fix: use `componentModel = "spring"` and inject `ObjectMapper` via constructor using a default method with `@Named`.

**Step 6: Verify**
```bash
mvn clean compile -pl backend
# Expect: BUILD SUCCESS — no MapStruct errors
```

---

## Task 11 — Backend: Case ↔ Client Assignment (bug3)

**Files:**
- Modify: `backend/src/main/java/com/lawfirm/application/dto/request/CreateCaseRequest.java`  (add `clientId`)
- Modify: `backend/src/main/java/com/lawfirm/application/dto/request/UpdateCaseRequest.java`  (add `clientId`)
- Modify: `backend/src/main/java/com/lawfirm/application/dto/response/CaseResponse.java`      (add `clientId`, `clientName`)
- Modify: `backend/src/main/java/com/lawfirm/application/mapper/CaseMapper.java`              (map client fields)
- Modify: `backend/src/main/java/com/lawfirm/application/service/CaseService.java`            (handle clientId)
- Modify: `backend/src/main/java/com/lawfirm/presentation/controller/CaseController.java`     (add PATCH endpoint)

**Step 1: Update DTOs**

In `CreateCaseRequest.java`, add at the end of the record fields:
```java
Long clientId   // optional — assigns client on creation
```

In `UpdateCaseRequest.java`, add at the end of the record fields:
```java
Long clientId   // optional — null = keep current, pass explicit null to unassign
```

Create new `AssignClientRequest.java`:
```java
package com.lawfirm.application.dto.request;

public record AssignClientRequest(Long clientId) {}
// clientId = null means unassign
```

**Step 2: Update CaseResponse**

In `CaseResponse.java`, add:
```java
Long clientId,
String clientName
```

**Step 3: Update CaseMapper**

In `CaseMapper.java`, add to the `toResponse` mapping:
```java
@Mapping(target = "clientId",   source = "client.id")
@Mapping(target = "clientName", source = "client",
         qualifiedByName = "clientFullName")
CaseResponse toResponse(Case caseEntity);

@Named("clientFullName")
default String clientFullName(Client client) {
    return client != null ? client.getFullName() : null;
}
```

**Step 4: Update CaseService**

In `createCase()`, after loading tribunal/category/lawyers, add:
```java
if (request.clientId() != null) {
    Client client = clientRepository.findByIdAndDeletedAtIsNull(request.clientId())
        .orElseThrow(() -> new ResourceNotFoundException("Client", request.clientId()));
    caseEntity.setClient(client);
}
```

Same pattern in `updateCase()`.

Add a new method `assignClient()`:
```java
@Transactional
public CaseResponse assignClient(Long caseId, Long clientId) {
    Case caseEntity = caseRepository.findByIdAndDeletedAtIsNull(caseId)
        .orElseThrow(() -> new ResourceNotFoundException("Case", caseId));
    if (clientId != null) {
        Client client = clientRepository.findByIdAndDeletedAtIsNull(clientId)
            .orElseThrow(() -> new ResourceNotFoundException("Client", clientId));
        caseEntity.setClient(client);
    } else {
        caseEntity.setClient(null);
    }
    return caseMapper.toResponse(caseRepository.save(caseEntity));
}
```

**Step 5: Add PATCH endpoint in CaseController**

```java
@PatchMapping("/{id}/client")
@PreAuthorize("hasAuthority('CASE_UPDATE')")
public ResponseEntity<CaseResponse> assignClient(
        @PathVariable Long id,
        @RequestBody AssignClientRequest request) {
    return ResponseEntity.ok(caseService.assignClient(id, request.clientId()));
}
```

**Step 6: Verify**
```bash
mvn clean verify -pl backend
# Expect: BUILD SUCCESS — all existing tests pass
```

---

## Task 12 — Frontend: Shared SearchableSelectComponent (bug1)

**Files:**
- Create: `frontend/src/app/shared/searchable-select/searchable-select.component.ts`
- Create: `frontend/src/app/shared/searchable-select/searchable-select.component.html`

**Step 1: Create component TypeScript**

```typescript
// frontend/src/app/shared/searchable-select/searchable-select.component.ts
import {
  Component, Input, Output, EventEmitter, OnDestroy, signal, inject, forwardRef
} from '@angular/core';
import { CommonModule } from '@angular/common';
import { ControlValueAccessor, NG_VALUE_ACCESSOR, FormsModule } from '@angular/forms';
import { HttpClient, HttpParams } from '@angular/common/http';
import { Subject, debounceTime, distinctUntilChanged, switchMap, of } from 'rxjs';

export interface SearchableOption {
  label: string;
  value: string | number;
  raw?: unknown;
}

@Component({
  selector: 'app-searchable-select',
  standalone: true,
  imports: [CommonModule, FormsModule],
  providers: [{
    provide: NG_VALUE_ACCESSOR,
    useExisting: forwardRef(() => SearchableSelectComponent),
    multi: true
  }],
  templateUrl: './searchable-select.component.html',
})
export class SearchableSelectComponent implements ControlValueAccessor, OnDestroy {
  @Input({ required: true }) endpoint!: string;
  @Input() searchParam = 'search';
  @Input() displayField!: string;
  @Input() valueField = 'id';
  @Input() placeholder = 'Rechercher...';
  @Input() multiple = false;
  @Input() additionalParams: Record<string, string> = {};

  @Output() selectionChange = new EventEmitter<SearchableOption | SearchableOption[] | null>();

  private http = inject(HttpClient);

  options = signal<SearchableOption[]>([]);
  loading = signal(false);
  open = signal(false);
  query = signal('');

  selectedValues = signal<SearchableOption[]>([]);

  private search$ = new Subject<string>();
  private destroy$ = new Subject<void>();

  private onChange: (val: unknown) => void = () => {};
  private onTouched: () => void = () => {};

  constructor() {
    this.search$.pipe(
      debounceTime(300),
      distinctUntilChanged(),
      switchMap(q => {
        this.loading.set(true);
        let params = new HttpParams().set(this.searchParam, q).set('page', '0').set('size', '20');
        Object.entries(this.additionalParams).forEach(([k, v]) => { params = params.set(k, v); });
        return this.http.get<{ content: unknown[] } | unknown[]>(this.endpoint, { params });
      })
    ).subscribe({
      next: (res) => {
        const items: unknown[] = Array.isArray(res) ? res : (res as { content: unknown[] }).content ?? [];
        this.options.set(items.map((item: unknown) => ({
          label: String((item as Record<string, unknown>)[this.displayField] ?? ''),
          value: (item as Record<string, unknown>)[this.valueField] as string | number,
          raw: item
        })));
        this.loading.set(false);
      },
      error: () => this.loading.set(false)
    });
  }

  onInput(event: Event): void {
    const q = (event.target as HTMLInputElement).value;
    this.query.set(q);
    this.search$.next(q);
    this.open.set(true);
  }

  select(option: SearchableOption): void {
    if (this.multiple) {
      const existing = this.selectedValues();
      const idx = existing.findIndex(v => v.value === option.value);
      const updated = idx > -1
        ? existing.filter((_, i) => i !== idx)
        : [...existing, option];
      this.selectedValues.set(updated);
      this.onChange(updated.map(v => v.value));
      this.selectionChange.emit(updated);
    } else {
      this.selectedValues.set([option]);
      this.query.set(option.label);
      this.open.set(false);
      this.onChange(option.value);
      this.selectionChange.emit(option);
    }
  }

  isSelected(option: SearchableOption): boolean {
    return this.selectedValues().some(v => v.value === option.value);
  }

  clear(): void {
    this.selectedValues.set([]);
    this.query.set('');
    this.onChange(this.multiple ? [] : null);
    this.selectionChange.emit(null);
    this.search$.next('');
  }

  writeValue(val: unknown): void {
    // Accept pre-selected values (e.g. in edit mode)
    if (!val) { this.selectedValues.set([]); return; }
    const arr = Array.isArray(val) ? val : [val];
    this.selectedValues.set(arr.map(v => ({
      label: String(v),
      value: v as string | number,
    })));
  }

  registerOnChange(fn: (val: unknown) => void): void { this.onChange = fn; }
  registerOnTouched(fn: () => void): void { this.onTouched = fn; }

  ngOnDestroy(): void { this.destroy$.next(); this.destroy$.complete(); }
}
```

**Step 2: Create component HTML**

```html
<!-- frontend/src/app/shared/searchable-select/searchable-select.component.html -->
<div class="relative w-full" (clickOutside)="open.set(false)">
  <!-- Input -->
  <div class="relative">
    <input
      type="text"
      [value]="query()"
      (input)="onInput($event)"
      (focus)="open.set(true)"
      [placeholder]="placeholder"
      class="w-full px-3 py-2 text-sm border border-gray-300 dark:border-gray-600 rounded-lg bg-white dark:bg-gray-700 text-gray-900 dark:text-white focus:outline-none focus:ring-2 focus:ring-blue-500"
    />
    @if (loading()) {
      <div class="absolute right-2 top-2.5">
        <div class="w-4 h-4 border-2 border-blue-500 border-t-transparent rounded-full animate-spin"></div>
      </div>
    }
    @if (selectedValues().length && !loading()) {
      <button type="button" (click)="clear()"
        class="absolute right-2 top-2 text-gray-400 hover:text-gray-600 dark:hover:text-gray-200 text-xs">✕</button>
    }
  </div>

  <!-- Multi-select chips -->
  @if (multiple && selectedValues().length) {
    <div class="flex flex-wrap gap-1 mt-1">
      @for (sel of selectedValues(); track sel.value) {
        <span class="px-2 py-0.5 text-xs bg-blue-100 dark:bg-blue-900 text-blue-800 dark:text-blue-200 rounded-full flex items-center gap-1">
          {{ sel.label }}
          <button type="button" (click)="select(sel)" class="hover:text-red-500">✕</button>
        </span>
      }
    </div>
  }

  <!-- Dropdown -->
  @if (open() && options().length) {
    <ul class="absolute z-50 mt-1 w-full max-h-60 overflow-y-auto bg-white dark:bg-gray-800 border border-gray-200 dark:border-gray-600 rounded-lg shadow-lg">
      @for (option of options(); track option.value) {
        <li
          (click)="select(option)"
          [class.bg-blue-50]="isSelected(option)"
          [class.dark:bg-blue-900]="isSelected(option)"
          class="px-3 py-2 text-sm cursor-pointer hover:bg-gray-50 dark:hover:bg-gray-700 text-gray-800 dark:text-gray-200 flex items-center justify-between"
        >
          {{ option.label }}
          @if (isSelected(option)) {
            <span class="text-blue-600 dark:text-blue-400 text-xs">✓</span>
          }
        </li>
      }
    </ul>
  }
</div>
```

> **Note:** `(clickOutside)` is not a built-in Angular directive. Either implement a simple host listener in the component or use `@HostListener('document:click', ['$event'])` inside the component to close the dropdown when clicking outside. See Angular docs for `HostListener`.

**Step 3: Verify**
```bash
cd frontend && pnpm run build --no-progress 2>&1 | tail -10
# Expect: no TypeScript errors
```

---

## Task 13 — Frontend: Replace Case Form Dropdowns & Add Client Field (bug1 + bug3)

**Files:**
- Modify: `frontend/src/app/features/cases/case-form/case-form.component.ts`
- Modify: `frontend/src/app/features/cases/case-form/case-form.component.html`
- Modify: `frontend/src/app/core/models/case.model.ts`
- Modify: `frontend/src/app/features/cases/case-detail/case-detail.component.html`

**Step 1: Update case.model.ts — add client fields**

In `CaseResponse` interface, add:
```typescript
clientId?: number;
clientName?: string;
```

In `CreateCaseRequest` and `UpdateCaseRequest` interfaces, add:
```typescript
clientId?: number;
```

Add new interface:
```typescript
export interface AssignClientRequest {
  clientId: number | null;
}
```

**Step 2: Update CaseService — add assignClient method**

In `frontend/src/app/services/case.service.ts`, add:
```typescript
assignClient(caseId: number, clientId: number | null): Observable<CaseResponse> {
  return this.http.patch<CaseResponse>(`/api/cases/${caseId}/client`, { clientId });
}
```

**Step 3: Update CaseFormComponent — replace dropdowns**

In `case-form.component.ts`:
- Remove `refDataService` from the component (no longer loading all reference data at init)
- Add `clientId` control to the form group with `null` default
- Import `SearchableSelectComponent`

In the `FormGroup` definition, add:
```typescript
clientId: [null]
```

In `case-form.component.html`:
- Remove the existing `<select>` for tribunal, caseType, caseCategory, and lawyer checkboxes
- Replace with `<app-searchable-select>` wired to the reactive form control

Example for tribunal:
```html
<!-- Replace existing tribunal <select> with: -->
<div class="form-group">
  <label class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1">
    Tribunal <span class="text-red-500">*</span>
  </label>
  <app-searchable-select
    formControlName="tribunalCode"
    endpoint="/api/tribunals"
    displayField="nameFr"
    valueField="code"
    placeholder="Rechercher un tribunal..."
  />
</div>
```

Example for lawyers (multiple):
```html
<div class="form-group">
  <label class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1">
    Avocats <span class="text-red-500">*</span>
  </label>
  <app-searchable-select
    formControlName="lawyerIds"
    endpoint="/api/lawyers"
    displayField="fullName"
    valueField="id"
    placeholder="Rechercher un avocat..."
    [multiple]="true"
  />
</div>
```

Add client field at the end (before submit buttons):
```html
<div class="form-group">
  <label class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1">
    Client assigné <span class="text-gray-400 text-xs">(optionnel)</span>
  </label>
  <app-searchable-select
    formControlName="clientId"
    endpoint="/api/clients"
    displayField="fullName"
    valueField="id"
    placeholder="Rechercher un client..."
  />
</div>
```

**Step 4: Add client section to case-detail.component.html**

Inside the case detail card (after the lawyers section), add:
```html
<!-- Client assigné -->
<div class="border-t border-gray-100 dark:border-gray-700 pt-3 mt-3">
  <dt class="text-xs font-medium text-gray-500 dark:text-gray-400 uppercase tracking-wide">
    Client assigné
  </dt>
  @if (caseData()?.clientName) {
    <dd class="mt-1 flex items-center justify-between">
      <span class="text-sm font-medium text-gray-900 dark:text-white">
        {{ caseData()?.clientName }}
      </span>
      @if (canUpdate()) {
        <div class="flex gap-2">
          <button (click)="showClientAssign.set(true)"
            class="text-xs text-blue-600 hover:text-blue-800 dark:text-blue-400">
            Modifier
          </button>
          <button (click)="unassignClient()"
            class="text-xs text-red-500 hover:text-red-700">
            Retirer
          </button>
        </div>
      }
    </dd>
  } @else {
    <dd class="mt-1 flex items-center justify-between">
      <span class="text-sm text-gray-400 dark:text-gray-500 italic">Aucun client</span>
      @if (canUpdate()) {
        <button (click)="showClientAssign.set(true)"
          class="text-xs text-blue-600 hover:text-blue-800 dark:text-blue-400">
          + Assigner
        </button>
      }
    </dd>
  }

  <!-- Inline client search (shown when editing) -->
  @if (showClientAssign()) {
    <div class="mt-2">
      <app-searchable-select
        endpoint="/api/clients"
        displayField="fullName"
        valueField="id"
        placeholder="Rechercher un client..."
        (selectionChange)="onClientSelected($event)"
      />
      <button (click)="showClientAssign.set(false)"
        class="mt-1 text-xs text-gray-400 hover:text-gray-600">Annuler</button>
    </div>
  }
</div>
```

**Step 5: Add client assign logic to CaseDetailComponent**

In `case-detail.component.ts`:
```typescript
showClientAssign = signal(false);

onClientSelected(option: SearchableOption | SearchableOption[] | null): void {
  if (!option || Array.isArray(option)) return;
  const id = this.caseData()?.id;
  if (!id) return;
  this.caseService.assignClient(id, option.value as number).subscribe({
    next: (updated) => {
      this.caseData.set(updated);
      this.showClientAssign.set(false);
    }
  });
}

unassignClient(): void {
  const id = this.caseData()?.id;
  if (!id) return;
  this.caseService.assignClient(id, null).subscribe({
    next: (updated) => this.caseData.set(updated)
  });
}
```

**Step 6: Verify**
```bash
cd frontend && pnpm run build --no-progress 2>&1 | tail -10
# Expect: BUILD SUCCESS
```

---

## Task 14 — Frontend: Payment Modal (bug4)

**Files:**
- Create: `frontend/src/app/features/financial/invoices/payment-modal/payment-modal.component.ts`
- Create: `frontend/src/app/features/financial/invoices/payment-modal/payment-modal.component.html`
- Modify: `frontend/src/app/core/models/financial.model.ts`
- Modify: `frontend/src/app/services/financial.service.ts`
- Modify: `frontend/src/app/features/financial/invoices/invoice-detail/invoice-detail.component.ts`
- Modify: `frontend/src/app/features/financial/invoices/invoice-detail/invoice-detail.component.html`

**Step 1: Update financial.model.ts**

Replace `InvoiceStatusRequest`:
```typescript
export interface InvoiceStatusRequest {
  status: InvoiceStatus;
  paymentMode?: PaymentMode;   // required when status = 'PAID'
  paymentDate?: string;        // required when status = 'PAID' (ISO date)
  paymentReference?: string;
}
```

Add `invoiceId` to `TransactionResponse`:
```typescript
export interface TransactionResponse {
  id: number;
  caseId: number;
  caseNumber: string;
  invoiceId?: number;          // ← NEW: set when auto-generated from invoice payment
  direction: TransactionDirection;
  // ... rest unchanged
}
```

**Step 2: Create PaymentModalComponent TypeScript**

```typescript
// frontend/src/app/features/financial/invoices/payment-modal/payment-modal.component.ts
import { Component, Input, Output, EventEmitter, signal } from '@angular/core';
import { CommonModule } from '@angular/common';
import { FormsModule } from '@angular/forms';
import { PaymentMode } from '../../../../core/models/financial.model';

export interface PaymentModalResult {
  paymentMode: PaymentMode;
  paymentDate: string;
  paymentReference?: string;
}

@Component({
  selector: 'app-payment-modal',
  standalone: true,
  imports: [CommonModule, FormsModule],
  templateUrl: './payment-modal.component.html',
})
export class PaymentModalComponent {
  @Input({ required: true }) invoiceNumber!: string;
  @Input({ required: true }) totalAmount!: number;
  @Output() confirmed = new EventEmitter<PaymentModalResult>();
  @Output() cancelled = new EventEmitter<void>();

  paymentMode = signal<PaymentMode | ''>('');
  paymentDate = signal(new Date().toISOString().substring(0, 10));  // today
  paymentReference = signal('');
  error = signal<string | null>(null);

  readonly paymentModeOptions: { value: PaymentMode; label: string }[] = [
    { value: 'CHECK',       label: 'Chèque' },
    { value: 'TRANSFER',    label: 'Virement' },
    { value: 'CASH',        label: 'Espèces' },
    { value: 'CREDIT_CARD', label: 'Carte bancaire' },
    { value: 'MONEY_ORDER', label: 'Mandat' },
  ];

  submit(): void {
    if (!this.paymentMode() || !this.paymentDate()) {
      this.error.set('Mode de paiement et date sont obligatoires');
      return;
    }
    this.confirmed.emit({
      paymentMode: this.paymentMode() as PaymentMode,
      paymentDate: this.paymentDate(),
      paymentReference: this.paymentReference() || undefined,
    });
  }
}
```

**Step 3: Create PaymentModalComponent HTML**

```html
<!-- payment-modal.component.html -->
<div class="fixed inset-0 z-50 flex items-center justify-center bg-black/50">
  <div class="bg-white dark:bg-gray-800 rounded-xl shadow-xl p-6 w-full max-w-md mx-4">
    <h3 class="text-lg font-semibold text-gray-900 dark:text-white mb-1">
      Marquer comme Payée
    </h3>
    <p class="text-sm text-gray-500 dark:text-gray-400 mb-4">
      Facture {{ invoiceNumber }} — {{ totalAmount | number:'1.2-2' }} MAD
    </p>

    @if (error()) {
      <div class="mb-4 p-3 bg-red-50 dark:bg-red-900/20 text-red-600 dark:text-red-400 rounded text-sm">
        {{ error() }}
      </div>
    }

    <div class="space-y-4">
      <!-- Payment Mode -->
      <div>
        <label class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1">
          Mode de paiement <span class="text-red-500">*</span>
        </label>
        <select
          [ngModel]="paymentMode()"
          (ngModelChange)="paymentMode.set($event)"
          class="w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-lg bg-white dark:bg-gray-700 text-gray-900 dark:text-white text-sm focus:outline-none focus:ring-2 focus:ring-blue-500"
        >
          <option value="">Sélectionner...</option>
          @for (opt of paymentModeOptions; track opt.value) {
            <option [value]="opt.value">{{ opt.label }}</option>
          }
        </select>
      </div>

      <!-- Payment Date -->
      <div>
        <label class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1">
          Date de paiement <span class="text-red-500">*</span>
        </label>
        <input
          type="date"
          [ngModel]="paymentDate()"
          (ngModelChange)="paymentDate.set($event)"
          class="w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-lg bg-white dark:bg-gray-700 text-gray-900 dark:text-white text-sm focus:outline-none focus:ring-2 focus:ring-blue-500"
        />
      </div>

      <!-- Reference (optional) -->
      <div>
        <label class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1">
          Référence <span class="text-gray-400 text-xs">(optionnel)</span>
        </label>
        <input
          type="text"
          [ngModel]="paymentReference()"
          (ngModelChange)="paymentReference.set($event)"
          placeholder="Ex: VIR-2026-001"
          class="w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-lg bg-white dark:bg-gray-700 text-gray-900 dark:text-white text-sm focus:outline-none focus:ring-2 focus:ring-blue-500"
        />
      </div>
    </div>

    <!-- Actions -->
    <div class="flex gap-3 mt-6 justify-end">
      <button (click)="cancelled.emit()"
        class="px-4 py-2 text-sm text-gray-600 dark:text-gray-400 hover:text-gray-900 dark:hover:text-white transition-colors">
        Annuler
      </button>
      <button (click)="submit()"
        class="px-4 py-2 text-sm font-medium text-white bg-green-600 hover:bg-green-700 rounded-lg transition-colors">
        Confirmer le paiement
      </button>
    </div>
  </div>
</div>
```

**Step 4: Wire PaymentModal into InvoiceDetailComponent**

In `invoice-detail.component.ts`:
- Add `showPaymentModal = signal(false)`
- Import `PaymentModalComponent` and `PaymentModalResult`
- Add method:
```typescript
onPaymentConfirmed(result: PaymentModalResult): void {
  const inv = this.invoice();
  if (!inv) return;
  this.financialService.updateInvoiceStatus(inv.id, {
    status: 'PAID',
    paymentMode: result.paymentMode,
    paymentDate: result.paymentDate,
    paymentReference: result.paymentReference
  }).subscribe({
    next: (updated) => {
      this.invoice.set(updated);
      this.showPaymentModal.set(false);
    },
    error: (err) => this.error.set(err.error?.message ?? 'Erreur lors du paiement')
  });
}
```

In `invoice-detail.component.html`:
- Replace the `SENT → PAID` button with:
```html
<button (click)="showPaymentModal.set(true)"
  class="px-3 py-1.5 text-xs font-medium text-white bg-green-600 hover:bg-green-700 rounded-lg">
  Marquer comme Payée
</button>
```
- Add the modal at the bottom of the template:
```html
@if (showPaymentModal() && invoice()) {
  <app-payment-modal
    [invoiceNumber]="invoice()!.invoiceNumber"
    [totalAmount]="invoice()!.totalAmount"
    (confirmed)="onPaymentConfirmed($event)"
    (cancelled)="showPaymentModal.set(false)"
  />
}
```

**Step 5: Verify**
```bash
cd frontend && pnpm run build --no-progress 2>&1 | tail -10
# Expect: BUILD SUCCESS
```

---

## Task 15 — Frontend: Audit History Diff Display (bug2)

**Files:**
- Modify: `frontend/src/app/core/models/case.model.ts`
- Modify: `frontend/src/app/features/cases/case-detail/case-detail.component.html`

**Step 1: Update AuditLogResponse model**

In `case.model.ts`, update the `AuditLogResponse` interface:
```typescript
export interface AuditLogResponse {
  id: number;
  action: string;
  resource: string;
  resourceId: string;
  username: string;
  metadata: string;
  oldValues?: Record<string, unknown>;   // ← NEW
  newValues?: Record<string, unknown>;   // ← NEW
  createdAt: string;
}
```

**Step 2: Add diff helper to CaseDetailComponent**

In `case-detail.component.ts`, add:
```typescript
// French labels for case fields shown in audit diff
readonly fieldLabels: Record<string, string> = {
  fullCaseNumber: 'N° dossier',
  tribunal:       'Tribunal',
  caseType:       'Type',
  caseCategory:   'Catégorie',
  status:         'Statut',
  priority:       'Priorité',
  lawyers:        'Avocats',
  client:         'Client',
  registrationDate: 'Date d\'enregistrement',
  caseDescription:  'Description',
  matterDescription: 'Objet',
  opposingParty:  'Partie adverse',
  outcome:        'Résultat',
};

getDiffRows(log: AuditLogResponse): { field: string; label: string; before: string; after: string }[] {
  if (!log.newValues) return [];
  const before = log.oldValues ?? {};
  const after  = log.newValues;
  return Object.keys(after)
    .filter(k => JSON.stringify(before[k]) !== JSON.stringify(after[k]))
    .map(k => ({
      field: k,
      label: this.fieldLabels[k] ?? k,
      before: this.formatValue(before[k]),
      after:  this.formatValue(after[k]),
    }));
}

private formatValue(val: unknown): string {
  if (val == null) return '—';
  if (typeof val === 'object') return JSON.stringify(val);
  return String(val);
}
```

**Step 3: Update history tab template**

In `case-detail.component.html`, replace the existing history tab content with:
```html
<!-- History Tab -->
@if (activeTab() === 'history') {
  <div class="space-y-3">
    @if (history().length === 0) {
      <p class="text-sm text-gray-400 dark:text-gray-500 text-center py-6">
        Aucun historique disponible.
      </p>
    }
    @for (log of history(); track log.id) {
      <div class="border border-gray-200 dark:border-gray-700 rounded-lg p-4">
        <!-- Event header -->
        <div class="flex items-center justify-between mb-2">
          <span class="text-xs font-semibold text-blue-600 dark:text-blue-400 uppercase tracking-wide">
            {{ log.action }}
          </span>
          <span class="text-xs text-gray-400 dark:text-gray-500">
            {{ log.username }} · {{ log.createdAt | date:'dd/MM/yyyy HH:mm' }}
          </span>
        </div>

        <!-- Before/After diff table -->
        @let diffRows = getDiffRows(log);
        @if (diffRows.length) {
          <table class="w-full text-xs border-collapse mt-2">
            <thead>
              <tr class="text-gray-500 dark:text-gray-400">
                <th class="text-left pb-1 w-1/4 font-medium">Champ</th>
                <th class="text-left pb-1 w-2/5 font-medium">Avant</th>
                <th class="text-left pb-1 w-2/5 font-medium">Après</th>
              </tr>
            </thead>
            <tbody class="divide-y divide-gray-100 dark:divide-gray-700">
              @for (row of diffRows; track row.field) {
                <tr class="text-gray-700 dark:text-gray-300">
                  <td class="py-1 pr-2 font-medium text-gray-500 dark:text-gray-400">{{ row.label }}</td>
                  <td class="py-1 pr-2 text-red-600 dark:text-red-400 line-through">{{ row.before }}</td>
                  <td class="py-1 text-green-600 dark:text-green-400">{{ row.after }}</td>
                </tr>
              }
            </tbody>
          </table>
        } @else if (log.metadata) {
          <!-- Fallback for old-format logs without snapshots -->
          <p class="text-xs text-gray-500 dark:text-gray-400 mt-1">
            Champs modifiés: {{ log.metadata }}
          </p>
        }
      </div>
    }
  </div>
}
```

**Step 6: Verify full stack**
```bash
# Backend
mvn clean verify -pl backend
# Frontend
cd frontend && pnpm lint && pnpm run build --no-progress 2>&1 | tail -10
# Expect: All tests pass, BUILD SUCCESS
```

---

## Final Verification

**Step 1: Run all migrations**
```bash
mvn flyway:migrate -pl backend
# Expect: Successfully applied 7 migrations (V56-V62)
```

**Step 2: Start backend and smoke test**
```bash
mvn spring-boot:run -pl backend &
# Wait for startup, then:
curl -X POST http://localhost:8080/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"admin123"}' | jq '.accessToken'

# Set TOKEN=<access_token> from above, then:

# Test searchable tribunal endpoint
curl "http://localhost:8080/api/tribunals?search=rabat&page=0&size=5" \
  -H "Authorization: Bearer $TOKEN" | jq '.content[].nameFr'

# Test invoice payment sync
# (create an invoice first via POST /api/financial/invoices, then PATCH status to PAID)
curl -X PATCH "http://localhost:8080/api/financial/invoices/1/status" \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"status":"PAID","paymentMode":"TRANSFER","paymentDate":"2026-03-03","paymentReference":"VIR-001"}'
# Expect: invoice status = PAID, a new REVENUE transaction created for that case
```

**Step 3: Start frontend and smoke test**
```bash
cd frontend && pnpm dev
# Open http://localhost:4200
# ✓ Case form tribunal dropdown: type "rabat" → see filtered results
# ✓ Case detail: "Client assigné" section visible with assign button
# ✓ Invoice detail: "Marquer comme Payée" opens modal
# ✓ Case history tab: shows before/after diff table on update events
```
